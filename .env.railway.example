# Variables de entorno para Railway
# Copia este archivo y configura las variables en el dashboard de Railway

# Base de datos Neon (requerido)
NEON_CONNECTION_STRING=postgresql://user:password@ep-xxx-xxx.us-east-2.aws.neon.tech/valoraciones?sslmode=require

# Configuración de entorno
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# Railway configura automáticamente estas variables:
# PORT - Puerto donde Railway espera que la app escuche (automático)
# RAILWAY_ENVIRONMENT - Entorno de Railway (automático)
# RAILWAY_SERVICE_NAME - Nombre del servicio (automático)

# Configuración de host (Railway maneja esto automáticamente)
HOST=0.0.0.0

# Middleware de proxy headers (opcional en Railway)
ENABLE_PROXY_MIDDLEWARE=false

# Configuración de Playwright
BROWSER_HEADLESS=true
PLAYWRIGHT_BROWSERS_PATH=/app/.cache/ms-playwright

# API externa para consulta RUC (opcional)
APIS_NET_PE_TOKEN=tu-token-de-apis-net-pe

# Configuración de timeout y reintentos
API_TIMEOUT=45000
RETRY_ATTEMPTS=2

# Configuración de CORS (opcional - ajustar según necesidad)
CORS_ORIGINS=*

# WhatsApp Business API (si aplica)
WHATSAPP_API_TOKEN=tu-token-whatsapp
WHATSAPP_PHONE_NUMBER_ID=tu-phone-id

# Redis (si usas Redis en Railway)
REDIS_URL=redis://red-xxx:6379

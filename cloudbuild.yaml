# Solo ejecutar este build cuando cambien archivos en backend/
# Configurar en Google Cloud Console: 
# Included files filter: backend/**
# Ignored files filter: frontend/**, README.md, *.md

steps:
  # Build the container image from backend directory
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-f', 'backend/Dockerfile', '-t', 'gcr.io/$PROJECT_ID/registro-valorizaciones:$COMMIT_SHA', 'backend']
    
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/registro-valorizaciones:$COMMIT_SHA']
    
  # Deploy container image to Cloud Run with environment variables
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
    - 'run'
    - 'deploy'
    - 'registro-valorizaciones'
    - '--image=gcr.io/$PROJECT_ID/registro-valorizaciones:$COMMIT_SHA'
    - '--region=southamerica-west1'
    - '--platform=managed'
    - '--allow-unauthenticated'
    - '--port=8080'
    - '--memory=4Gi'
    - '--cpu=2'
    - '--timeout=900s'
    - '--max-instances=10'
    - '--cpu-boost'
    - '--set-env-vars=DEBUG=false,LOG_LEVEL=INFO,HOST=0.0.0.0,NEON_CONNECTION_STRING=$(NEON_CONNECTION_STRING),ENVIRONMENT=production'

images:
  - 'gcr.io/$PROJECT_ID/registro-valorizaciones:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
  substitution_option: 'ALLOW_LOOSE'